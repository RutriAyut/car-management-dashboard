import { css } from "@emotion/css";

export const smRow = css({
  display: "flex",
  flexDirection: "row",
  gap: "16px",
});
export const sm = css({
  width: "32px",
  height: "32px",
  backgroundColor: "#0d28a6",
  borderRadius: "50px",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
});

export const footerRow = css({
  justifyContent: "space-between",
  alignItems: "start",
  display: "flex",
});

export const footerCol = css({
  display: "flex",
  gap: "16px",
  flexDirection: "column",
});
