import { css } from "@emotion/css";

export const styLogo = (color) =>
  css({
    width: "100px",
    height: "34px",
    flexShrink: "0",
    background: color,
  });

export const styLogoAdmin = css({
  width: "34px",
  height: "34px",
  background: "#cfd4ed",
});
